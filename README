my_env
======
This tool builds my ~/dotfiles based on runtime options. Example usage:
./my_env.py --target={ linux | osx }

The only argument declares the host OS family. This argument is required.


The .bash_aliases* files are not included in the public repository.

